# netlify.toml - minimal configuration for plain HTML/CSS/JS

[build]
  # Publish the current folder (root of the repository)
  publish = "."

# Optional: set the default branch to main
[context.production.environment]
  BRANCH = "main"
